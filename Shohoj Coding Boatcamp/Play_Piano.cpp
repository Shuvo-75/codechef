#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    cin.ignore();
    vector<string> words(t);

    for(int i = 0; i < t; i++)
    {
        getline(cin, words[i]);
    }

    

    
    return 0;
}